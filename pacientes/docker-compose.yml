server:
  build: ./server
  command: npm start
  links:
    - mongodb
  ports:
    - "8080:8080"
  volumes:
    - ./server:/var/www/pacientes
  environment:
    - DB=mongodb://mongodb/pacientes
mongodb:
  image: mongo
